# Netlify Environment Variables Template
# Copy this to your Netlify dashboard if you need environment-specific variables

# Production Environment
# Site Settings > Build & Deploy > Environment > Edit Variables

# Example (not required for current setup):
# NODE_ENV=production
# VITE_API_URL=https://api.yourdomain.com

# The current setup does not require any environment variables
# This file is provided for future reference if needed
